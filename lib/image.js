const fs = require('fs')
const path = require('path')
const fetch = require('node-fetch') 

module.exports = { 
image = {
async functions(retries = 0) {
    let maxRetries = 30
    let options = maxRetries
    let image = null
    try {
        return 
    } catch (e) {
        if (retries >= maxRetries) {
            return m.reply('error')
        }
                
        image = await handler(options, retries++);
    }
   

    return 
}

}
}
